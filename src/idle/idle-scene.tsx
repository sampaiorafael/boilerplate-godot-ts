import { signal, property, tool, onready, node } from '../../decorators';

@tool
export default class IdleScene extends godot.Node2D {

    _input (event: any): void {

    }
    
	_ready (): void {
    }

    _process (delta): void {
    }

}